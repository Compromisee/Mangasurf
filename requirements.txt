# Everything needed to run Mangasurf with all interfaces.
#
# Prefer the package extras if you are installing from source:
#     pip install -e ".[all]"      GUI + TUI + tray
#     pip install -e ".[gui]"      desktop app only
#     pip install -e ".[tray]"     background/system-tray mode
#
# This file is the "give me everything" list.

# --- core -------------------------------------------------------------
requests>=2.31
beautifulsoup4>=4.12
rich>=13.0
Pillow>=10.0
fpdf2>=2.7
EbookLib>=0.18
curl_cffi>=0.7.0; python_version<'3.14'

# --- desktop app and reader (PyQt6 and pywebview) ---------------------
PyQt6>=6.5.0
pywebview>=5.0

# --- full-screen terminal UI -----------------------------------------
textual>=0.60

# --- system tray / background mode -----------------------------------
pystray>=0.19

# --- servers: phone UI (server.py) and OPDS catalog (opdsserve.py) ---
flask>=3.0
pytest>=7.0
